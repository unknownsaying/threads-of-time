Class Sphere
  Dim Radius;
  ReDim  fai, theta;
  Typedef  X = Radius * cos(fai)
  Typedef  Y = Radius * sin(fai)
  Typedef  Z = Radius * cos(fai) * sin(theta)
  Typedef  T = Radius * cos(fai) * cos(theta)
End Class
